# Skill: Encoding Preservation

- Keep files UTF-8.
- Write shell edits with explicit UTF-8 encoding.
- Preserve punctuation and non-ASCII characters.
- Check for corruption markers like `�`, `â€”`, `â€™`, and `Ã` after bulk edits.
