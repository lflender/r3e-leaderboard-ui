# Skill: Cache Busting

- Bump cache-busting versions for all changed static assets.
- Update HTML references when editing `*.js` or `*.css` loaded by pages.
- Prefer a central cache-busting constant if one exists.
- Keep edited client-delivered assets versioned together.
