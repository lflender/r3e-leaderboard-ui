# Skill: Architecture & Reuse

- Avoid duplicate logic; reuse helpers or extract shared modules.
- Keep responsibilities separate: UI rendering, data access, formatting, and transformations.
- Do not use inline styles when CSS classes can solve the layout.
- Load optional data asynchronously with single-flight promises.
