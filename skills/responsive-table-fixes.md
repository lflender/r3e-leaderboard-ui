# Skill: Responsive Table Fixes

- Fix table overflow at the widest breakpoint where it occurs.
- Let the fix cascade down rather than applying only at mobile.
- Override `.results-table` `min-width` only when needed for narrow containers.
- Use the project breakpoints: `1300px`, `1000px`, `768px`, `480px`.
